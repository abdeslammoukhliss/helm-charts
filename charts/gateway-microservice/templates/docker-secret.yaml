apiVersion: v1
kind: Secret
metadata:
  name:{{include "gateway-microservice.fullname" .}}-docker-secret
  namespace: {{.Values.namespace}}
type: kubernetes.io/dockerconfigjson
data:
  .dockerconfigjson: {{ .Values.imagePullSecrets.data | quote }}
